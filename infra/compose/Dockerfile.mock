FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir fastapi uvicorn pydantic

COPY apps/mock_provider /app/apps/mock_provider

ENV PYTHONPATH=/app

EXPOSE 8001

CMD ["uvicorn", "apps.mock_provider.main:app", "--host", "0.0.0.0", "--port", "8001"]
